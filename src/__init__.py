"""Clinical Terminology Mapper package.

Keeping this file lets Python treat the src folder as a package so
commands like `python -m src.main` and `python -m unittest` can import
modules with `from src.pipeline import ...`.
"""
